# Dependencies of the book in docs/ (mkdocs.yml) only. Nothing here enters the
# C++ library build, and nothing here is needed to run the conformance tests.
#
#   python -m venv .venv-docs
#   .venv-docs/Scripts/pip install -r requirements-docs.txt   # Windows
#   .venv-docs/bin/pip install -r requirements-docs.txt       # Linux/macOS
#   mkdocs serve
mkdocs-material==9.7.7
