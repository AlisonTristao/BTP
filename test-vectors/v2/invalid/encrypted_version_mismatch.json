{
  "format": "btp-test-vector-v2",
  "id": "encrypted_version_mismatch",
  "description": "ENCRYPTED stays set but the version octet is downgraded from 2 to 1. ENCRYPTED implies version 2. The CRC is recomputed because decode() verifies the envelope CRC before reaching this rule; without recomputing, the vector would trip CrcMismatch instead of exercising the error under test.",
  "base": "aead_telemetry_gcm",
  "mutations": [
    {
      "operation": "set_u8",
      "offset": 4,
      "value": "0x01",
      "recompute_crc": true
    }
  ],
  "expected_error": "EncryptedVersionMismatch"
}
